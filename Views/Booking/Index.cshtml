@model IEnumerable<hjemmeside2.Models.Booking>
@{
    ViewData["Title"] = "Oversigt list over bestilte tider";
}
<h2>@ViewData["Title"]</h2>
        
        <a class="btn btn-success" asp-action="Create">Opret en ny tid</a>
        <br>
        <br>
        <table class="table table-hover"> 
            <tr> 
                <th>@Html.DisplayNameFor(Models => Models.FirstName) </th>
                <th>@Html.DisplayNameFor(Models => Models.LastName) </th>
                <th>@Html.DisplayNameFor(Models => Models.Email) </th>
                <th>@Html.DisplayNameFor(Models => Models.PhoneNumber) </th>
                <th>@Html.DisplayNameFor(Models => Models.DateTimeny) </th>
                <th>@Html.DisplayNameFor(Models => Models.Tidspunkt) </th>
                <th>@Html.DisplayNameFor(Models => Models.Combobox) </th>
                <th>@Html.DisplayNameFor(Models => Models.Comments) </th>
                <th> Ændringer </th>
            </tr>
            @foreach (var item in Model)
            {
                <tr >  
                    <td>@Html.DisplayFor(modelItem => item.FirstName) </td> 
                    <td>@Html.DisplayFor(modelItem => item.LastName) </td> 
                    <td>@Html.DisplayFor(modelItem => item.Email) </td> 
                    <td>@Html.DisplayFor(modelItem => item.PhoneNumber) </td> 
                    <td>@Html.DisplayFor(modelItem => item.DateTimeny) </td>
                    <td>@Html.DisplayFor(modelItem => item.Tidspunkt) </td>  
                    <td>@Html.DisplayFor(modelItem => item.Combobox) </td>  
                    <td>@Html.DisplayFor(modelItem => item.Comments) </td>
                    <td> <a asp-action="Edit" class="btn btn-primary"  asp-route-id="@item.ID">Edit</a>
                <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.ID">Delete</a>
                <a asp-action="Details" class="btn btn-info" asp-route-id="@item.ID">Details</a> </td>  
                </tr>
            }
            
        </table>
     

 